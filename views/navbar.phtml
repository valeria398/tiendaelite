<!-- ===================== NAVBAR ===================== -->

<nav class="navbar">

    <!-- LOGO -->
    <div class="nav-logo">
        <?= htmlspecialchars($logo ?? 'Elite') ?>
    </div>


    <!-- CATEGORIAS -->
    <div class="nav-categorias">

        <?php
        $categorias = [
            'todos' => 'Todos',
            'ropa' => 'Ropa',
            'zapatos' => 'Zapatos',
            'accesorios' => 'Accesorios'
        ];

        $categoria_activa = $categoria_activa ?? 'todos';
        ?>

        <?php foreach ($categorias as $key => $nombre): ?>

            <button
                class="nav-cat <?= $categoria_activa === $key ? 'activo' : '' ?>"
                data-cat="<?= $key ?>"
                onclick="filtrarCategoria('<?= $key ?>')"
            >
                <?= $nombre ?>
            </button>

        <?php endforeach; ?>

    </div>


    <!-- LOGIN -->
    <?php if (!isset($_SESSION['usuario'])): ?>

        <button
            class="btn-login-toggle"
            id="btn-toggle"
            onclick="abrirLogin()"
        >
            Iniciar Sesion
        </button>

    <?php else: ?>

        <div class="nav-usuario" id="nav-usuario" style="display: flex;">

            <div class="avatar" id="avatar-usuario">
                <?= strtoupper(substr($_SESSION['usuario']['nombre'], 0, 1)) ?>
            </div>

            <span id="nombre-usuario">
                <?= htmlspecialchars($_SESSION['usuario']['nombre']) ?>
            </span>

            <button
                class="btn-salir"
                onclick="cerrarSesion()"
            >
                Salir
            </button>

            <button
                class="btn-carrito"
                onclick="abrirCarrito()"
            >
                Carrito

                <span
                    class="badge"
                    id="badge-carrito"
                    <?= empty($_SESSION['carrito']) ? 'style="display:none"' : '' ?>
                >
                    <?= count($_SESSION['carrito'] ?? []) ?>
                </span>

            </button>

        </div>

    <?php endif; ?>

</nav>